/** @type {import('next').NextConfig} */
const nextConfig = {
  // The appDir option is no longer needed in Next.js 15
  // as it's enabled by default for the app directory
}

module.exports = nextConfig
